name: multiplicative_constraint
version: 0.1.0
authors:
  - Sethu Iyer <stuehieyr@gmail.com>

description: |
  Constraint partitioning engine by Sethu Iyer (github.com/sethuiyer).
  Heat-kernel spectral annealer for arbitrary segmentable objectives.

crystal: ">= 1.8, < 2.0"

dependencies:
  grpc:
    github: jeremey/grpc.cr
  protobuf:
    github: jeremey/protobuf.cr

homepage: https://github.com/sethuiyer/spectral-multiplicative-framework

license: Restricted
